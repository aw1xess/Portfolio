import React from "react";
import { keyframes } from "styled-components";

const fadeIn = keyframes`
  0% {
    opacity: 0;
  }
  100% {
    opacity: 1;
  }
`;

const Home = () => {
	return <div></div>;
};

export default Home;
